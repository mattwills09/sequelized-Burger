<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-wasdevoured="{{devoured}}">
		{{#if devoured}}NOT EATEN!{{else}}EATEN!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
